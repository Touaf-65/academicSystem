server:
  port: 8093

security:
  internal-secret: my-super-internal-secret

spring:
  application:
    name: teacher-service

  datasource:
    url: jdbc:h2:mem:teacher_db
    driverClassName: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
    hostname: localhost

  client:
    service-url:
      defaultZone: http://localhost:8771/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"